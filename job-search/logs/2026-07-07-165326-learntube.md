# Job Application Log -- LearnTube -- 2026-07-07

## Job Description

We're looking for engineers with hands-on experience across most of the following technologies:

Python Backend Development (3+ years)
FastAPI (2+ years of production experience required)
PostgreSQL
MongoDB
Redis
Docker
Celery or equivalent async task queues
REST APIs & Microservices
AWS/GCP
GitLab CI/CD

The selected Sr. Backend engineer will ship the backbone that powers 4 million + learners in 64 countries, owning APIs that crunch 1 billion learning events & the AI that supports it with <200 ms latency.

What You'll Do:

At LearnTube, we're pushing the boundaries to revolutionize how the world learns. As a Senior Backend Engineer, your roles and responsibilities will include:

Shipping Micro-services – Build FastAPI services that handle ≈ 800 req/s today and will triple within a year (sub-200 ms p95).
Power Real-Time Learning – Drive the quiz-scoring & AI-tutor engines that crunch millions of events daily.
Design for Scale & Safety – Model data (Postgres, Mongo, Redis, SQS) and craft modular, secure back-end components from scratch.
Deploy Globally – Roll out Dockerised services behind NGINX on AWS (EC2, S3, SQS) and GCP (GKE) via Kubernetes.
Automate Releases – GitLab CI/CD + blue-green / canary = multiple safe prod deploys each week.
Own Reliability – Instrument with Prometheus / Grafana, chase 99.9 % uptime, trim infra spend.
Expose Gen-AI at Scale – Publish LLM inference & vector-search endpoints in partnership with the AI team.
Ship Fast, Learn Fast – Work with founders, PMs, and designers in weekly ship rooms; take a feature from Figma to prod in < 2 weeks.

What makes you a great fit?

Must-Haves (Applicants without these skills are unlikely to move forward):

3+ yrs Python back-end experience (FastAPI)
Strong with Docker & container orchestration
Hands-on with GitLab CI/CD, AWS (EC2, S3, SQS) or GCP (GKE / Compute) in production
SQL/NoSQL (Postgres, MongoDB) + You've built systems from scratch & have solid system-design fundamentals

Nice-to-Haves
k8s at scale, Terraform,
Experience with AI/ML inference services (LLMs, vector DBs)
Observability: Prometheus, Grafana, OpenTelemetry

Why LearnTube?
Build AI products at global scale – Your work will power learning experiences for 4M+ learners across 64 countries.
Work directly with experienced founders – Collaborate closely with founders who are deeply involved in product and engineering decisions, giving you ownership and a direct impact on the product roadmap.
Learn fast – Join a high-performing team where you'll own features end-to-end and solve challenging backend and distributed systems problems.
Recognized AI startup – LearnTube is a Google Top 20 AI Startup and part of Google's AI First Startup Accelerator, with access to cutting-edge AI technology and mentorship.

## Questions

(none)

## Extra Context (NOTEs)

none

## Tone

startup casual

## What They're Looking For

- Python + FastAPI as the production workhorse (800 req/s today, tripling, sub-200ms p95) -- ownership of services from scratch, not just feature work
- Postgres, Mongo, Redis, SQS data modeling at event scale, plus async task queues (Celery or equivalent) for background processing
- Docker + k8s (GKE/EKS) + AWS or GCP in production, GitLab CI/CD with blue-green/canary -- multiple safe deploys per week
- Observability ownership tied to uptime: Prometheus, Grafana, OTel, not just setup but actually chasing SLAs
- LLM inference + vector search exposure (nice-to-have, but it's the product direction -- the AI tutor and quiz engines are the core)

## Fit Score

Fit: 10/15 direct matches, 4 adjacent -- strong

## Answers

(no questions submitted -- pitch generated instead)

## Pitch

I've been building Python backends and owning infra across four roles since 2022, and LearnTube's stack is almost a direct match for what I've been working in.

At The One World I migrated their core API layer to FastAPI from AWS Amplify, which cut API latency by 60% and infra costs by 35%. I also built a PostGIS proximity query over 5,000+ shop locations that held sub-500ms response times under concurrent load. That was my first production FastAPI service, built from scratch.

At Aftershoot AI I stood up their full observability stack: Prometheus, Grafana, OTel Collector, and Loki, tracking p95 latency and error rates across Go microservices and a Python image processing pipeline. I also wrote integration tests against staging Postgres and Redis, which brought coverage to 85% and cut post-deploy support tickets by 20% over three months.

At Credible Informatics I built a RAG pipeline from scratch. Scrapy crawlers ingested 10+ financial sources on a cron schedule, we generated OpenAI embeddings and stored them in a vector DB, and every LLM response was grounded in real market data. I kept API latency under 200ms by caching LLM responses in Redis and parallelizing GPT calls with async I/O.

Most recently at One2N I provisioned ephemeral EKS, ECS, and EC2 environments via Terraform for chaos engineering test runs, and wired up distributed tracing, metrics, and logs across all compute substrates.

Two honest gaps: I haven't used Celery specifically (I've done async background jobs in Python using other patterns, and the model is familiar) and my CI/CD experience is GitHub Actions rather than GitLab. The concepts transfer directly. GCP/GKE I haven't touched in production, but k8s is k8s and my EKS work maps over without much friction.

The AI tutor and quiz-scoring engines are what make this role interesting to me. I've built RAG pipelines and LLM-grounded APIs. Owning inference endpoints at 4M user scale is exactly the kind of problem I want to be working on.

## Why Hire Me

- FastAPI in production at The One World: migrated 12 REST endpoints, cut latency 60%, built the service from scratch. Directly maps to "shipping FastAPI services" requirement.
- Prometheus + Grafana + OTel Collector + Loki at Aftershoot AI: not just setup, but dashboards tracking p95 latency used to cut post-deploy tickets. Directly maps to their 99.9% uptime + observability requirement.
- RAG pipeline at Credible: OpenAI embeddings, vector DB, Redis caching to hold 200ms latency. Directly maps to "expose Gen-AI at scale" and LLM/vector DB nice-to-have.
- EKS + ECS + EC2 provisioned via Terraform at One2N: ephemeral isolated environments, full lifecycle automation. Directly maps to "deploy globally on k8s + AWS" requirement.
- Integration tests against staging Postgres and Redis at Aftershoot: 85% coverage, measurable reliability improvement. Maps to their SQL/NoSQL at scale requirement.
- PostGIS proximity query at The One World: sub-500ms under load over 5K+ locations. Signals real Postgres data modeling, not just basic CRUD.

## Gaps (not surfaced in answers)

- MongoDB: listed in skills but no specific project or work evidence
- Celery: no direct experience; mentioned honestly in pitch
- GitLab CI/CD: have GitHub Actions, bridged in pitch
- GCP/GKE: have AWS/EKS k8s, bridged in pitch
